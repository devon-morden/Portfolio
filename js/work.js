const left = document.getElementById('left-chevron');

const right = document.getElementById('right-chevron');

const projects = document.querySelectorAll('.project');

// const inactiveSlide = inactiveSlides[i];

function nextSlide() {
  console.log('I heard that!');
  //get rid of project-visible
  //add project-hidden
  //show next section
  projects.forEach(() => { //go through all of the sections
    let i = 0 //initialize index counter
    let project = projects[i]; //initialize project: 
  });
}

function prevSlide() {
  console.log("Je l'ai entendu!");
}